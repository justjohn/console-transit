#!/usr/bin/env node

var NextBus = require('../src/raw/nextbus').NextBus,
	MBTA = require('../src/raw/mbta').MBTA,
	nextBus = new NextBus("mbta");


/* nextBus.routeList().then(function(routes) {
	console.log(routes);
}); */

/* nextBus.predictions("23151").then(function(config) {
	console.log(config);
}); */



var mbta = new MBTA();

mbta.subways().then(function(data) {
	console.log(data);
});

mbta.subway('Red').then(function(data) {
	console.log(data);
});

/* mbta.platforms().then(function(data) {
	console.log(data);
}); */

